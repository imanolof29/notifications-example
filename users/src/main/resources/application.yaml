server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ejercicio3
    username: postgres
    password: ""
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update